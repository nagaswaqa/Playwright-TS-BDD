node_modules/
.env*
! .env.example
storage/*.json
test-results/
playwright-report/
blob-report/
playwright/.cache/
.features-gen/
allure-results/
allure-report/
logs/
*.log

# Cached login storageState — grants admin access to the live SIS, do not commit.
.auth/

# Playwright HTML report + ZAP security reports.
reports/

# ZAP runtime state (api key, session metadata)
.zap/

# Throwaway helpers and run logs
.tmp-*
