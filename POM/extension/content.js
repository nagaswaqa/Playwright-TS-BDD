// This content script runs on the page.
// Currently the logic is injected dynamically from popup.js for simplicity in passing arguments.
// We can move complex logic here if the message passing architecture is preferred.
console.log('POM Generator Content Script Loaded');
